# BACKLOG

One row per commit. The loop takes the top `OPEN` row, fixes exactly that,
runs `npm run check`, and marks the row `DONE` with the commit hash. On a
failing check the row is marked `BLOCKED` with the reason and the working
tree is reverted.

Status is one of `OPEN`, `DONE`, `BLOCKED`, `PROPOSED`. This list is defects
first; feature rows only with Sam's explicit approval and a one-line why,
placed after all OPEN defect rows.

`PROPOSED` rows come from a product review (rows 21-33: review/product-1,
2026-09-16) and are not approved. The loop only ever takes `OPEN` rows, so it
skips them. Each ends with its size and `LOOP` or `SUPERVISED`. Sam approves a
`LOOP` row by changing its status to `OPEN`. A `SUPERVISED` row removes a
screen, changes a flow or restructures `index.html`: it stays `PROPOSED` until
it is done by hand in a supervised session, so the loop never picks it up. A
row that opens with **DECISION** needs Sam's answer written into it first.
Rejecting a row is deleting it. They are sequenced: each leaves the app
coherent on its own, and later rows assume the earlier ones landed.

Rows are worked in the order they appear, not by number: the top `OPEN` row
is next. The `#` is a stable id used in commit subjects, so a row inserted
later keeps its own number rather than renumbering the rows below it.

| #  | Status | Commit | Title |
|----|--------|--------|-------|
| 34 | DONE   | 34aa2c1 | completedToday is not reset if the app stays open across midnight; the day-rollover effect only runs on mount. **Size: SMALL.** |
| 14 | DONE   | df96dff | At 390px the RPE chips do not fit on any weighted exercise. The set row needs 394px (44 check + 60 weight + 52 reps + seven 28px chips + gaps) inside a 326px card, the chip group cannot shrink, and the card clips it: RPE 9 is cut in half and RPE 10 is off-screen and cannot be tapped (`ExerciseCard` set row, `index.html` ~761; measured in Playwright at 390x844, last chip ends at x=427). Progression is RPE-gated, so this hides the top of the scale the program runs on. Bodyweight rows fit only because they have no weight field. Fix: let the set row wrap and give the chip group its own full-width line under the weight/reps inputs, for every exercise, so all seven chips are visible at 390px and at 375px. Change nothing else about the chips. **Size: SMALL.** |
| 15 | DONE   | bb4df98 | West of UTC, every cold start forgets today's sessions. The day-rollover effect (`index.html` ~834) compares `getLocalDateKey(new Date(cycleState.lastDate))` with today, but `lastDate` is a `YYYY-MM-DD` key and `new Date("YYYY-MM-DD")` parses as UTC midnight -- in America/Chicago that is the previous local day, so the keys never match and `completedToday` is reset to `[]` on every launch. Reproduced: finish Session A, reload, and the header goes from "1 session today" back to "Ready to train" with `wk-st.completedToday` now `[]`. Consequences: "Double Day Available" disappears after a relaunch, the second session of a day is not treated as one (no carry advisory), and `isDoubleDay` is never set, so Modified C is never recommended. Fix: when `lastDate` is a 10-character key compare it to today's key directly (otherwise parse it as local noon, as `daysSince` at ~908 already does). **Size: SMALL.** |
| 16 | DONE   | 5f4a050 | The day after an A+B double day the app recommends Session C, not Modified C. `getRec` (~838) only returns `modC` while `completedToday.length>=2`, and the day-rollover effect has emptied `completedToday` by then, so Modified C is only ever offered as a third session on the double day itself. The program is A+B same day, then Modified C next. Reproduced with state `{lastSession:"B", isDoubleDay:true, completedToday:["A","B"], lastDate:<yesterday>}`: Next Up shows C. Fix: in `getRec` and `getRecReason`, recommend `modC` whenever `isDoubleDay` is set, regardless of `completedToday`; `saveAndFinish` already clears the flag when the following session is saved. `getRecReason`'s "Two sessions today" is wrong the next morning, so make it "Double day -> Modified C". Depends on row 15. **Size: SMALL.** |
| 17 | DONE   | 1721e4d | Two different tests are both called "second session today". The workout and complete screens use `completedToday.length>=1` (~1147, ~1214), which is right. `startSession` (~850), the readiness Continue handler (~1063) and `getPreSessionContext` (~334) use `cycleState.isDoubleDay`, which is only set once the second session has been saved and stays set until the session after that is saved. So the Game Plan says nothing on the real second session of a day, and on the first session of the next day it shows "Second session today -- consider lighter carries" and gives carries the "maintain or lighten" reason (reproduced: `~/abc-loop-logs/review/38-gameplan-day-after-double-day.png`). Fix: those three sites use `completedToday.length>=1`, the test the workout screen already uses. **Size: SMALL.** |
| 18 | DONE   | 3375136 | The debrief names the session you just finished as the next one. `renderSessionComplete` calls `getRec()` (~1205) before anything is saved, so it returns the recommendation that led to this session: finish Session A and the screen says "Next: Session A" twice (debrief card and footer), and the balance advisory is checked against the same stale value (~1226). Fix: derive Next from the cycle state `saveAndFinish` is about to write (the same `lastSession` / `completedToday` / `isDoubleDay` / `preTgu` values), so finishing A shows B, finishing the second cycle session of a day shows Modified C, and finishing TGU shows the pre-TGU recommendation. Do not change what `saveAndFinish` writes. **Size: SMALL.** |
| 19 | DONE   | af68a28 | The debrief's per-exercise advice ignores the session it is debriefing. `renderSessionComplete` calls `getProgression(ex.id, logs, ...)` (~1219) with `logs` that do not yet contain today's sets, so it repeats the pre-session advice: do 3x10 at 45 because the app said to and the debrief says "start at 45 next session"; grind out 45x6 at RPE 10 and it says the same. Fix: for the debrief only, evaluate progression against today's completed sets prepended to `logs` (newest-first), writing nothing. The clipboard summary's "Progression status" line (~1258) has the same stale input and gets the same one. **Size: SMALL.** |
| 20 | DONE   | 81e407e | The rest timer counts ticks, not time. `RestTimer` (~484) subtracts one from `rem` per `setInterval` callback; iOS stops timers while the screen is locked or the PWA is in the background, so a phone put down between sets comes back showing most of the rest still to run, and the beep is late by however long it slept. Reproduced with Playwright's clock: 3s awake then 60s suspended, and the header's elapsed clock (which is wall-clock) reads 1:04 while the rest timer reads 1:25. Fix: record the end time when the timer starts or resumes, derive `rem` from `Date.now()` on every tick and on `visibilitychange`, beep once when it crosses zero. Pause keeps working. **Size: SMALL.** |
| 1  | DONE   | 8d18721 | The X button silently discards the session: remove `clearDraft()` from `home()`, add it to the Readiness Cancel path (both `saveAndFinish` paths already call it), re-hydrate `draftSession` after `home()` so the banner shows without a cold boot, and make `startSession` refuse to overwrite an existing draft (offer resume or discard instead). |
| 9  | DONE   | c7d9f32 | In-progress drafts are only persisted by a 5s `setInterval` (`index.html` ~835), so a crash, an OS kill or a tab discard loses up to 5 seconds of logged sets — the user re-enters weight and reps they already confirmed. Write the draft through on set completion as well as on the interval. **Size: HOURS.** |
| 2  | DONE   | 16c3b5e | `ExerciseCard` reads `getExE1RM` backwards: the array is newest-first, but the card takes `e1rmData[length-1]` as current, so the displayed e1RM and the trend arrow both describe the oldest entries. |
| 3  | DONE   | dbb57c8 | The dashboard frequency grid fills column-major (`grid[weekIdx*7+dow]`) but the CSS grid renders row-major, so every day lands in the wrong cell. |
| 4  | DONE   | 7a7b14a | `tryIDBRecovery` only looks for `localStorage.getItem(k) === null`, so a key that is present but holds corrupt JSON never triggers recovery and the app boots empty while a good IndexedDB copy sits unused. |
| 5  | DONE   | 64fa9bf | History renders `logs.slice(0, 50)` while `saveAndFinish` retains 100, so the oldest half of the user's history is unreachable with no paging or "show more". |
| 10 | DONE   | 9a865c3 | `sw.js` answers a navigation from the cache and only revalidates in the background (`return cached \|\| networkFetch`, `sw.js:48`), so the first open after every deploy renders the previous build and the new one appears a launch later. |
| 13 | DONE   | 727a097 | `sw.js` caches any navigation response that is `response.ok`, so a captive-portal or hotel-wifi sign-in page returning 200 is written into the cache under the app's own URL and served on the next offline open. Introduced by row 10. Two conditions fix it: fall back to the cached page unless `response.ok`, and only `cache.put` a response whose `type` is `basic`. **Size: SMALL.** |
| 6  | DONE   | 9760049 | `sw.js` hardcodes `CACHE_NAME = 'abc-workout-v2'` (`sw.js:1`) while the app version string lives in `index.html` (banner `v21` at :38, home header at :925) -- two unrelated numbers, neither of which anything bumps. `activate` only purges caches whose name differs, so the precached entries survive every deploy and a stale asset is served until the name is edited by hand. Fix: one version value both files carry -- `index.html` declares it and `sw.js` builds `CACHE_NAME` from its own copy (no build step, so they cannot share a symbol) -- bumped with any `index.html` change, plus a gate check in tests/ that fails when `index.html` changed and the version did not, and when the two copies disagree. Leave `version: 21` in the export payloads alone: that is the export schema field and `applyImport` never reads it. Note: the commit that lands this is the first one the new check runs against, so bump the version in it. |
| 8  | DONE   | 994c626 | `applyImport` writes `KEYS.logs` without enforcing newest-first order, so an out-of-order backup file silently corrupts every helper that slices history by index (`getGhostSets`, `getProgression`, `shouldDeload`, `getFatigueTrend`). |
| 11 | DONE   | f64ba23 | Row 9 writes the draft through when a set is completed, but the same handler still leaves un-completing a set or editing a logged weight/rep value to the 5s `setInterval`, so a crash within those 5s restores the set the user just corrected or cleared. |
| 7  | DONE   | dd5d8b2 | Prefilled weight/reps from history render dimmed (placeholder style) until the user edits them or taps the check circle; either action makes them solid and logs them. One tap still logs a set. No confirm prompt. Size small. |
| 12 | DONE   | a073779 | FEATURE (approved by Sam): on the debrief screen, show a one-tap "Export backup" button every 10th completed session, reusing the existing `exportBackup`. Why: a manual JSON export is the only durable copy of the user's history. |
| 21 | DONE     | 308f113 | **DECIDED: yes -- the two qualifying sessions must be at the same weight.** Today one session at a new weight that hits top reps at target RPE triggers the next +5 (verified against `getProgression`: 40x10@8, 40x10@8, then a single 45x10@8 gives "Progression criteria met -> 50 lbs"), so the 2-session rule only ever delays the first jump. Fix: require `h0.weight===h1.weight` in both branches of `getProgression`, plus a fixture. / Why: it is the program's one loading rule. / SMALL / LOOP once answered. |
| 22 | DONE     | e0b69f4 | **DECIDED: keep 1000 sessions.** `saveAndFinish` deletes the oldest log past 100 and says so afterwards in an `alert`; `addManualLog` does it silently; Home warns from 90. Raise all three to 1000, advisory at 900. / Why: 100 sessions is about 150 KB of a roughly 5 MB quota and about six months of training -- the cap destroys the only copy of the history for no storage reason, and every history-derived number quietly stops being all-time. Touches data-loss tokens `saveAndFinish` and `S.set(KEYS.logs, ...)`. / SMALL / LOOP -- the SUPERVISED hold was for the decision, which is now answered, so the loop may take it; it still touches `saveAndFinish` and `S.set(KEYS.logs, ...)`, so REPORT.md needs the data-loss note. |
| 23 | DONE     | a87e84c | Remove the camera heart-rate monitor: section 7 (PPG engine), `HRMonitor`, the heart button on the rest timer, `showHR` -- about 245 lines, a sixth of the file. Keep the `// 4.` and `// 6.` headers the test harness keys on. / Why: the program autoregulates on RPE, not heart rate; a 15-second fingertip-over-the-lens reading is a two-handed job in the middle of a rest; and its "Next Set" button leaves the floating timer running. / HOURS / SUPERVISED. |
| 24 | DONE     | 1c7078c | Remove the Dashboard screen and the Load Targets sheet, with their Home footer buttons and the target button in the workout header. / Why: no dashboard panel feeds a decision the program asks for, and two of the four are wrong today (weekly-volume bars never get a height, so all twelve render as the same 4px stub; the frequency grid matches on the UTC date, so evening sessions land on the next day); Load Targets is a static week 1-12 table that the app's own suggested load replaced once history existed. If this row is rejected, those two defects become OPEN rows. / SMALL / SUPERVISED. |
| 25 | DONE     | c1d26f1 | Remove the PR subsystem: the Records screen and its footer button, the "PR territory / At PR weight / N lbs away" line on the Game Plan, in-set trophies, the PR badge, the "PR Session!" header, `sessionPRs`, and the `prs` field on new logs (old logs keep theirs). / Why: under double progression the record is simply the current working weight, so following the app's own +5 lights up a PR on every set, and the board reads "Carries: best e1RM 140 lbs, 40 reps" because carry sets store yards in `reps`. If this row is rejected, that carry arithmetic becomes an OPEN row. Touches `saveAndFinish`. / HOURS / SUPERVISED. |
| 26 | DONE     | 21338a0 | Remove the derived-metric readouts: Home's Insights block (7-day volume, workload ratio, Reduce Load banner), the Complete screen's Session Insights card and the tonnage figure in its header, and "e1RM: N" with its trend arrow on collapsed exercise cards. Move the balance advisory to one line under the Recommended card. `getFatigueTrend` on the pre-session screen stays as the single fatigue signal. / Why: none of these feeds the progression rule; tonnage is dominated by carry yards (60 lb x 40 yd = 2,400 per set against 450 for a squat set), which skews volume and the ratio; "Low load" after a missed week is the guilt the program rejects; the within-session check calls a Session C that hits its own targets (8, 9, 8) "systemic" fatigue; and three fatigue detectors with three wordings on three screens is the clearest stapled-together seam in the app. / HOURS / SUPERVISED. |
| 27 | DONE     | 195ce64 | Home: replace the three stat cards with one line inside the Recommended card ("Last: C, 2d ago - 3 sessions in 7 days") and drop the "Double Day Available" chip, which looks like a button and is not one (say "1 done today" in that same line instead). / Why: "Next Up" repeats the Recommended card directly beneath it and "Last" repeats that card's own "Last completed" line; with rows 24-26 landed, Home is gear, Recommended, five session tiles and History, with no scrolling. / SMALL / LOOP. |
| 28 | DONE     | edbf45c | Fold the warm-up picker into the Game Plan: one row showing the suggested warm-up, tap to expand its steps, switch to another or skip; the Standard/Circuit toggle moves there too; delete the warm-up screen and Home's Time Available selector. / Why: the selector's only effects are capping the warm-up suggestion and preselecting Circuit, and both are then one tap away at the point they apply; today "Start Lifting" logs a warm-up the user was never shown, and Circuit is only reachable through a screen titled "Pick Your Warm-Up". / HOURS / SUPERVISED. |
| 29 | DONE     | ca81b28 | **DECIDED: yes -- one question, Rough / OK / Good.** Fold the check-in into the Game Plan as one row -- Rough / OK / Good, default OK -- stored in the existing shape as 1-1-1 / 2-2-2 / 3-3-3 so every consumer and every old log keeps working; delete the Check-In screen and the "N sessions - phase" chips. / Why: the three answers are only ever summed, and the sum only matters at 4 or under (hold loads) and 6 or under (warm-up choice); today an untouched default is stored as a real 6/9. Pre-lift goes from three screens and 3-6 taps to one screen and 2. / HOURS / SUPERVISED. |
| 30 | DONE     | 40987de | Exercise card: remove the YouTube reference block (the dashed "Add YouTube Reference" button, the embed and the URL editor; leave `KEYS.videos` alone in storage, export and import) and replace the per-set "Last:" lines with one "Last time: 40x10 @8, 40x10 @8, 40x10 @8" line under the header. / Why: the card spends a full-width control in the middle of every set on pasting a URL, five months in, when Form cues already cover form; last time is currently shown four ways at once (header, per-set line, dimmed prefill, placeholder); and row 14 needs the height back. / HOURS / SUPERVISED. |
| 31 | DONE     | e11907a | **DECIDED: yes -- an RPE chip tap logs the set.** A chip tapped on an unlogged set marks it complete and starts the rest timer; the check circle still logs a set without an RPE; chips on a logged set only change its RPE. / Why: halves the taps in a session (18 to 9 for nine sets), makes the number the program depends on the natural way to log, and fixes adaptive rest silently not applying when the check is tapped before the RPE. / SMALL / SUPERVISED. |
| 32 | DONE     | 4b49f95 | **DECIDED: no -- remove the AI prompt.** Remove the button, `copySummary` and the `KEYS.analysisTs` writes (imports that carry the key still load). / Why: it is another app's job bolted onto the debrief -- the largest single block in the Complete screen -- and it re-reports the e1RM, workload-ratio and deload figures that rows 25-26 remove. / SMALL / SUPERVISED. |
| 33 | DONE     | c65810c | Sweep, after the rows above (the pure-function half landed inside rows 24-26, which deleted each function as its last caller went; this row is the rest): delete the pure functions nothing renders any more, with their fixtures (expected: `calcE1RM`, `getExE1RM`, `calcE1RMTrends`, `calcVolume`, `calcTotalVolume`, `calcWeeklyVolumes`, `calcACWR`, `calcRPEDistribution`, `shouldDeload`, `classifyFatigue`, `detectPRs`, `getAllTimePRs` -- only those whose callers are really gone); the inline `RestTimer` branch and the `timer` state that the floating timer made unreachable; the duplicate "Default Carry Type" in Settings (the in-card toggle writes the same key); the rest timer's Pause button; the second "Next:" in the Complete footer; and bring the header comment's section list up to date. / Why: in a single-file app, dead code is something every later reader and every loop session has to hold in their head. Deleting the fixtures of deleted functions is not weakening a test, but a person should be the one to say so. / HOURS / SUPERVISED. |
| 35 | DONE     | 87574bf | Unit fixtures for the import path: `validateLogs` keeps well-formed entries, drops malformed ones and never reorders; `applyImport`'s sort leaves logs newest-first for a shuffled input. If those functions are not reachable from the `// 4.` to `// 6.` slice, exercise them in Playwright through `page.evaluate` instead of moving code. Tests only, no `index.html` change. / Why: these guard the only copy of the history and nothing tests them today. **Size: SMALL.** LOOP. |
| 36 | DONE     | a52df27 | Smoke journey: finish a session, reload, and check three things -- the session is in History, "Last completed" names it, and Next Up has advanced. Tests only. / Why: `saveAndFinish` has no automated check at all. **Size: SMALL.** LOOP. |
| 37 | DONE     | af973c8 | Smoke journey: every discard path leaves logs and the recommendation untouched. Cover the X then banner Discard, Cancel on the Game Plan, and Finish with no sets then confirm Discard. Assert `wk-logs` and `wk-st` in localStorage are byte-identical before and after each. Tests only. / Why: guards the rule that a discarded session never counts as done. The deleted-session case is deliberately not here; it fails today and lands with row 43. **Size: SMALL.** LOOP. |
| 38 | DONE     | 38176b1 | Smoke journey: export a backup, wipe all data from Settings, import the same file, and check History and Next Up match what they were before. Tests only. / Why: import and export are the only restore path and have no test. **Size: SMALL.** LOOP. |
| 39 | DONE     | 0691dce | Smoke journey: on a fresh session tap an RPE chip on an unlogged set and check the set is marked done and the rest timer starts; tap a different chip on that logged set and check only the RPE changes; tap Rough on the Game Plan Feel row and check the stored readiness is 1-1-1. Tests only. / Why: rows 29 and 31 changed the two most-used controls and have no automated check. **Size: SMALL.** LOOP. |
| 40 | OPEN     |  | Smoke journey: use Playwright's clock to finish a session, advance past local midnight with the app still open, and check the header falls back from "1 session today" to "Ready to train" while the recommendation stays on the next session. Tests only. / Why: rows 15 and 34 were both day-rollover defects and neither has a regression test. **Size: SMALL.** LOOP. |
| 41 | OPEN     |  | `tests/gate.js`: a change to `index.html` that only touches comment lines or whitespace no longer requires an `APP_VERSION` bump; any other change still does, and the two-copies-agree check is unchanged. Add a fixture for the comment-only case. / Why: a version bump for a comment invalidates the service-worker cache for nothing and makes every loop run bump twice. **Size: SMALL.** LOOP. |
| 42 | OPEN     |  | Delete the `STYLES.statCard` leftover if it is still there, and any other `STYLES` key with no reader, found by grepping each key for a use. One deletion per key, no other change. / Why: the row 33 sweep missed one, so the loop review keeps rediscovering it. **Size: SMALL.** LOOP. |
| 43 | PROPOSED |  | Derive the cycle state from the logs instead of storing it. `lastSession`, `lastDate`, `completedToday`, `isDoubleDay` and `preTgu` become a pure function of `logs` (newest-first) and today's date, placed in section 5 with fixtures; `KEYS.state` stays in storage and export for old backups but is no longer read for the recommendation. Deleting a session from History, wiping history, importing a file and discarding a draft then all give the right Next Up with no rollback code. Fixtures must include: delete the newest log, delete every log, import an older file, TGU in the middle of a cycle, A+B same day then the next day. The smoke test for "delete a session and Next Up steps back" lands with this row. / Why: `deleteLog` (`index.html` ~626) removes a log without touching the stored state, so the recommendation keeps counting a session the history no longer shows; a stored copy of something the logs already say can only drift. Touches data-loss tokens `saveAndFinish`, `applyImport` and `S.set(KEYS.logs, ...)`; REPORT.md needs the note. **Size: HOURS.** SUPERVISED. |
| 44 | PROPOSED |  | **DECIDED: Firestore, keyed by a passphrase Sam chooses.** Silent backup mirror: after each `saveAndFinish`, `applyImport` and `deleteLog`, write the log array to Firestore under a document path derived from the passphrase (hash it; never keep it in the page source); on boot, if local logs are empty and a passphrase is set, offer restore. Settings shows "last backed up" and a field to set the passphrase. Local storage stays the primary copy and the app works offline; a failed write retries on the next open and never blocks a save. Needs Sam to create the Firebase project, enable Firestore and paste the web config; the loop must not deploy rules. / Why: history lives only in this browser's storage, which iOS can clear, and an export is never going to be done by hand. Touches data-loss tokens `saveAndFinish`, `applyImport`, `validateLogs` and `S.set(KEYS.logs, ...)`. **Size: HOURS.** SUPERVISED. |

## Harness work (supervised session, not loop rows)

These edit files the loop cannot write, so they wait for `sh sup.sh`. They are
listed here so the next session starts from the plan rather than from memory.

- Fresh-context reviewer agent that runs `npm run check` itself and ends with
  CLEAN or FINDINGS in 20 lines; `run.sh` parses the verdict and gives the
  worker one fix pass on FINDINGS.
- Review engine: parallel single-lens agents (navigation and taps, phone
  ergonomics, program logic, data integrity, feature gap, persona walk of the
  preview) that write deduplicated `PROPOSED` rows with evidence, impact and
  effort, and keep a `REJECTED` section with reasons.
- `run.sh`: short PR body (verdict, one line per row, phone checklist, then
  the full report); stale-lock healing by PID; a clear message for a dirty
  tree; auto-merge of a PR that passed `npm run check`, got CLEAN and touches
  no data-loss token, then a check that the live `APP_VERSION` matches.
- `revert.sh <row>`: one command to revert a row's commit and its BACKLOG
  commit.
- Guard audit: try unusual write forms against `.claude/hooks/guard.js`, since
  the workers run with `--dangerously-skip-permissions`.
- `CLAUDE.md`: say when to suggest a loop run (any `OPEN` rows after a backlog
  change) and a review-engine run (after a change to navigation, layout,
  progression or storage, and before choosing new features); record the
  auto-merge rule and its exclusion of data-loss tokens; add "a correction
  becomes a rule in the same commit".
- Data-loss note check: at review time, fail a branch whose diff touches a
  data-loss token without a matching note in REPORT.md.
