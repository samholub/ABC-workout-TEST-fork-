{
  "name": "abc-cycle-tracker-harness",
  "version": "1.0.0",
  "private": true,
  "description": "Test harness for the ABC Cycle Tracker PWA. The app itself ships as index.html + sw.js and has no build step; nothing here is deployed.",
  "scripts": {
    "gate": "node tests/gate.js",
    "test": "node tests/unit.test.js",
    "smoke": "node tests/smoke-run.js",
    "check": "npm run gate && npm run test && npm run smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
